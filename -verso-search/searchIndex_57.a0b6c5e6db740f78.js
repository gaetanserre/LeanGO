window.docContents[57].resolve({"/Some-examples/#Formalization-of-stochastic-iterative-optimization-algorithm--Some-examples--RankOpt":{"contents":"The RankOpt algorithm has been introduced in  and is a sophisticated decision-based global optimization algorithm. It is based on the notion of a ranking rule, which is a function induced by another function f and is defined as\n\nr_f (x, y) = \\begin{cases}\n1 & \\text{if } f(x) > f(y) \\\\\n0 & \\text{if } f(x) = f(y) \\\\\n-1 & \\text{if } f(x) < f(y)\n\\end{cases}.\n\nRanking rules define equivalence classes of functions that share the same induced ranking. To use RankOpt, one needs to define a countable set of ranking rules \\mathcal{R} (e.g. a subset of the ranking rules induced by continuous functions). The algorithm samples points x such that, for any ranking rule r \\in \\mathcal{R} that is consistent with the observed data, 0 \\le r(x, \\arg \\max_{1 \\le i \\le n} f(X_i)). The set of such points is the set of potential maximizers of the algorithm and is defined as\n\n\\left \\{x \\in \\alpha \\; \\middle| \\; \\exists r \\in \\mathcal{R}, \\; \\mathcal{L}_n(r) = 0 \\land 0 \\le r(x, \\arg \\max_{1 \\le i \\le n} f(X_i))\\right\\},\n\nwhere α is a measurable space and \\mathcal{L}_n(r) is the ranking loss of r on the observed data, defined as\n\n\\mathcal{L}_n(r) \\triangleq \\frac{2}{n (n + 1)} \\sum_{1 \\le i \\le j \\le n} \\mathbb{I}\\left[r(X_i, X_j) \\neq r_f(X_i, X_j)\\right].\n\nNote that RankOpt does not require to know r_f explicitly as it is evaluated only on observed data points.\n\nIt can be represented in our framework as a special case of the Decision structure, where the decision rules return 1 if the candidate solution is in the set of potential maximizers and 0 otherwise:\n\n\n\nNote that RankOpt requires the set of potential maximizers to have non-zero measure at each iteration, ensuring that the algorithm can sample from it. This is a non-trivial assumption that depends on the choice of the initial probability measure μ, the function to optimize, and the \\sigma-algebra on the search space.\n\nThe RankOpt algorithm for global optimization.\nThis algorithm uses a ranking approach to optimize an unknown function. It maintains a hypothesis\nclass 𝓡 of ranking rules. It starts with an arbitrary probability measure μ as initial\ndistribution and samples from the set of points that could be optimal according to ranking rules\nconsistent with the observed data (Malherbe et al., 2017).\n\nThe term RankRule is defined as the subtype of \\{-1, 0, 1\\}-valued functions that are jointly measurable:\n\n\n\n","context":"Formalization of stochastic iterative optimization algorithm\u0009Some examples","header":"2.4. RankOpt","id":"/Some-examples/#Formalization-of-stochastic-iterative-optimization-algorithm--Some-examples--RankOpt"}});